// filter returns a new array
// can manipulate the size of new array
// returns array based on condition

const arr = [1,2,3,4,5,6,7];
aboveThree = arr.filter(function(number){
    return number >3;
});
console.log(aboveThree);

