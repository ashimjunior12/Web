Why to use useReducer hook?

- As our application grows in size, it's going to be very hard to manage everything with just useState.That's where state management libraries like Redux comes into play. The problem with these libraries is that they require quite a bit of boilerplate code. So, react released useReducer hook. 
